[pytest]
testpaths = tests
markers =
    live: hits real Gemini API (skipped by default — opt-in with -m live)
    e2e: Playwright browser tests
addopts = -m "not live" --strict-markers
asyncio_mode = auto
